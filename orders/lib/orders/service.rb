module Orders
  class Service
    def call(commands)
    end

    private
    def register_order(cmd)
    end

    def cancel_order(cmd)
    end

    def expire_order(cmd)
    end

    def ship_order(cmd)
    end
  end
end
